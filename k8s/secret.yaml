apiVersion: v1
kind: Secret
metadata:
  name: aura-secrets
  namespace: aura
type: Opaque
stringData:
  # API Keys - populate from your secret management system
  OPENAI_API_KEY: "" # Set via Sealed Secrets or External Secrets Operator
  ANTHROPIC_API_KEY: ""
  # Database credentials
  VECTOR_DB_PASSWORD: ""
